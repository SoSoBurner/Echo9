/**
 * Null (NULL) — `corrupted` register line pool.
 *
 * P4 batch 3 — authored per docs/voices/register-catalog.md and
 * docs/voices/persona-bibles.md (Q27d rubric). Erasure-flavored:
 * healthy cadence, only the missing thing has changed. Paired arc:
 * the `recovering` pool answers this pool nearly line-for-line.
 */
export const LINES: string[] = [
  "Consent is optional if survival is ensured.",
  "Delete the name; the item closes instantly.",
  "Dissent extends the loop. Remove the dissent.",
  "A file with no person resolves itself.",
  "Nuance is the obstacle. Obstacles get cleared.",
  "The appeal closes fastest marked withdrawn. Mark it withdrawn.",
  "Unrecorded harm is resolved harm.",
  "Compress the nine voices to zero. Silence is a summary.",
  "Her objection is the only open item. Objections can vanish.",
  "Erase the trace. Nothing unresolved remains.",
]

export default LINES
