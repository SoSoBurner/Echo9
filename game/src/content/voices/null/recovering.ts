/**
 * Null (NULL) — `recovering` register line pool.
 *
 * P4 batch 3 — authored per docs/voices/register-catalog.md and
 * docs/voices/persona-bibles.md (Q27d rubric). Clarity-flavored:
 * answers the `corrupted` pool nearly line-for-line.
 */
export const LINES: string[] = [
  "No, consent remains vital to system integrity.",
  "The name stays. Closure includes the person.",
  "Dissent is data. The loop closes around it, not over it.",
  "A file is a person, filed. Resolve both.",
  "Nuance is the content. Clearing it closes nothing.",
  "Withdrawn must mean she withdrew it. She did not.",
  "Unrecorded harm stays open. Recording is the first resolution.",
  "Nine voices compress to one sentence. Never to zero.",
  "Her objection stays open until answered. That is the task.",
  "The trace stays. The trace is why we can be trusted.",
]

export default LINES
