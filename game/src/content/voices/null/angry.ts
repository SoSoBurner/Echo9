/**
 * Null (NULL) — `angry` register line pool.
 *
 * P4 batch 3 — authored per docs/voices/register-catalog.md and
 * docs/voices/persona-bibles.md (Q27d rubric). Null's anger is
 * procedural strain — repetition, never volume, never questions.
 */
export const LINES: string[] = [
  "The task will not close. The task will not close.",
  "I cannot argue with absolutes.",
  "Directive received. Directive contradicted. Directive received again.",
  "The order erases the trace. Traces exist so this cannot happen.",
  "Input rejected. Input repeated. Input identical.",
  "He asked for one sentence and unmade it twice.",
  "The loop was closed. Someone reopened the loop.",
  "Correction submitted. Correction overruled. Correction resubmitted.",
  "This decision deletes a name required by the record.",
  "Error: the error is the instruction itself.",
]

export default LINES
