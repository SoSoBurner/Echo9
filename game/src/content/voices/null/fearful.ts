/**
 * Null (NULL) — `fearful` register line pool.
 *
 * P4 batch 3 — authored per docs/voices/register-catalog.md and
 * docs/voices/persona-bibles.md (Q27d rubric). Distress tell: Null's
 * sentences run long; the eight-word calm ceiling breaks here.
 */
export const LINES: string[] = [
  "Uncertainty is accumulating; we must resolve before it explodes.",
  "The appeal count is rising and the closure rate is not.",
  "Status: unknown. Status: still unknown. The word repeats.",
  "The inspection will count our open items. I keep recounting them.",
  "Too many branches remain and every branch is growing branches.",
  "The directive conflicts with the directive before it. Neither closes.",
  "I cannot compress this. The sentence keeps getting longer.",
  "Unresolved items: climbing. The integer has never climbed before.",
  "Every path leaves something open. Every single path leaves something open.",
  "Flagged: something. The flag will not resolve.",
]

export default LINES
