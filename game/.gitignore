# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OpenAI API key files — the portrait pipeline reads OPENAI_API_KEY from env
# only, but a stray .openai-key or .env dropped into the repo would be trivial
# to leak. Belt-and-suspenders.
.openai-key
.env
.env.*
!.env.example
