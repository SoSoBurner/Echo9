# Design Discovery — Rubric Score Sheet

This rubric captures the "the game wants to become something else" signal. Run every 4th Deep session against the accumulated notes from the prior 4 weeks. Do not use this rubric for a single session — it needs a multi-session pattern.

| Field | Value |
|---|---|
| Cycle ID (4-week block) | YYYY-Www to YYYY-Www |
| Sessions in cycle | (list session IDs) |
| Build range (git SHA start → end) | → |
| Date completed | |

## Score dimensions (1 = signal absent, 5 = signal screaming)

| # | Dimension | Score (1–5) | One-line note |
|---|---|---|---|
| 1 | **Pattern strength** — are the same emergent behaviors surfacing across multiple Deep sessions? | | |
| 2 | **Design misalignment** — is the current design fighting against something the game does well? | | |
| 3 | **Cost-to-pivot** — how expensive is embracing the new direction? (1 = trivial, 5 = rewrite) | | |
| 4 | **Value-of-pivot** — how much would the ship-gate signal (§17 unsolicited comments) improve? | | |
| 5 | **Aesthetic coherence** — does the new direction still honor the 5 pillars (identity, comfort, complicity, silence, ritual)? | | |

## Recommended direction

- **What** (the new direction, in one sentence):
- **Why** (what emergent pattern justifies it):
- **What we'd cut** (design commitments the pivot would sunset):
- **What we'd add** (new commitments the pivot would introduce):

## Decision

- [ ] Pivot — draft a PLAN.md amendment for user review.
- [ ] Hold — keep watching; re-run in 4 more weeks.
- [ ] Reject — the pattern is noise; document and move on.
