# Ship-Gate Dry-Run — 2026-07-08

**Build (git SHA):** `3281970` — feat(v5): DonutChart + AlertCrawler
**Executor:** Claude Code (Opus 4.7) — solo, no human co-pilot on rubric scoring
**Verdict:** **DO NOT SHIP.** Two structural blockers (Phase 5 signal, Kleenex Phase 4) prevent RC tag. Phase 1 clean; Phase 2 partial by platform; Phase 3 failed on infra (not app).

The gate is a chain — Phase 5's honest NO would invalidate every earlier PASS even if 2/3 were green.

---

## Phase 1 — Mechanical — **PASS**

| Step | Command | Result |
|---|---|---|
| 1 | `npx tsc --noEmit` | exit 0 |
| 2 | `npx oxlint` | 1 grandfathered warning (`vite.config.ts:1` triple-slash) — no new |
| 3 | `npx vitest run` | 645 / 645 in 89 s |
| 4 | `npx playwright test --project chromium` | 13 / 13 in 6.7 min (includes 100-iter soak) |
| 5 | traceability-invariant | 4 / 4 (§11 spine intact) |
| 6 | perf-baseline-check | first-load gzip ~105.82 KB (budget 275 KB; 5× headroom); heap 15.35 MB baseline; save p95 0.70 ms |
| 6.5 | `npm run verify:subpath` | file:// + http-root + http-subpath all boot |

Verdict: **CLEAR.** No mechanical blocker.

---

## Phase 2 — Cross-browser — **PARTIAL (platform gap)**

| Step | Project | Result |
|---|---|---|
| 7 | mercyMarginSlice @ firefox | 3 / 3 |
| 7 | mercyMarginSlice @ chromium | (covered in Phase 1) |
| 7 | mercyMarginSlice @ webkit | **ENGINE UNAVAILABLE** — `ms-playwright/` has chromium + firefox only on Windows |
| 8 | persistenceRoundTrip @ firefox | 2 / 2 |
| 8 | persistenceRoundTrip @ webkit | **ENGINE UNAVAILABLE** |
| 9 | `docs/test-flakes.md` skim | empty log — no recurring flake |

Firefox runtime: 2.5 min for both specs. Sub-3-min cross-browser is a healthy CI budget.

**Caveat, not a blocker:** webkit is Playwright's Safari surrogate. Windows Playwright ships webkit binaries but they are not currently installed. Before an RC tag, either (a) install webkit locally via `npx playwright install webkit`, or (b) run the RC gate from a macOS/Linux CI runner where the trio is standard. This dry-run treats webkit as "verify on CI" — not "safe to skip."

Verdict: **PARTIAL.** Passes on the two engines available. Full trio needs a CI runner or webkit install before the real RC.

---

## Phase 3 — Soak — **FAILED (infrastructure, not app)**

**Command:** `SOAK_ITERATIONS=500 npx playwright test src/tests/e2e/soakTest.spec.ts --project chromium`
**Result:** 1 failed. `page.goto('/') → net::ERR_CONNECTION_REFUSED at http://localhost:5173/` mid-iteration.

**Root cause:** Vite dev server (Playwright `webServer`) died mid-soak. The failing call is `page.goto`, not a heap-budget assertion. The 100-iteration soak inside Phase 1 completed with **heap growth 0.00 MB from 15.35 MB baseline** and `save p95 0.70 ms` — the app itself is not the failure surface.

**Why this happened:** the soak runs ~500 boot+choice cycles at ~7-8 s each = ~65-70 min. Vite's dev server accumulates client watchers, HMR sockets, and cached bundles across sessions; long-lived instances hit socket / handle exhaustion. This is a known ceiling for Vite-served long-soak Playwright runs and is called out in `game/CLAUDE.md` (SOAK_ITERATIONS budgeting note, measured 2026-07-07).

**Two mitigations for the real RC:**

1. **Serve the built `dist/` via `npm run play` or `vite preview`** instead of the dev server. Preview is a static file server — no HMR, no watchers, no memory growth from tooling. Update `playwright.config.ts` `webServer.command` to point at `vite preview` for `SOAK_ITERATIONS >= 500`.
2. **Bucket the soak** — five sequential 100-iter runs, harness restart between buckets. Total wall time similar; each Vite process stays inside its ceiling.

Neither mitigation is on the critical path for the pre-RC audit itself — both are `docs/ship-gate.md:25` improvements. But this dry-run cannot claim Phase 3 PASS without them.

Verdict: **FAILED on harness, unproven on budget.** The app's soak behavior in the successful 100-iter run is well within budget; the 500-iter budget hasn't actually been measured because the harness didn't survive the run. Blocker for RC; not a blocker for continued development.

---

## Phase 4 — Heuristic + solo playtest — **BLOCKED (Kleenex requires human)**

### 11. Kleenex playtest — **NOT PERFORMED**

The Kleenex protocol (`docs/playtest/self-playtest-kleenex.md`, referenced by Phase 4 step 11) requires "24h code break beforehand" and a solo Q1 walk-through with fresh eyes. Both preconditions are unavailable to an AI executor in the same session that built V5. This is honest: I cannot run this step. A human playtester is required before RC.

### 12-16. Analytical rubric assessment (in lieu of human scoring)

Each rubric below is a rubric card — 1–5 per dimension. I score these analytically against the current build (post-V5, pre-C-track content fill). Scores marked `NR` = not-ratable without a live human playtester. A live Kleenex session will overwrite these.

#### 12. Fun review — `docs/playtest/rubrics/fun-review.md`

| # | Dimension | Score | Note |
|---|---|---|---|
| 1 | Incentive chain clarity | 3 | Priority Tasks + Financial ring visible at maturity 3; onboarding still doesn't tell the player *why* to care |
| 2 | Tension | 2 | Only one directive loop (mercyMargin) implemented; no Q1 arc yet — no compounding threat |
| 3 | Surprise reveals | NR | Requires human reaction |
| 4 | Player clarity | 4 | LogDock + AlertCrawler + result cards now form a coherent WHY-NOW spine |
| 5 | Appeal vs Enjoyment | 3 | HUD chrome (donut, portraits, crawler) reads as intended; enjoyment gated on content fill |
| 6 | Flow | NR | Requires 30+ min human session |

**Highest-leverage change:** Ship Track C (Q1 12-week directive arc) before any further chrome. §17 pillars can't earn 10 unsolicited comments off one directive loop. Cost: L.

#### 13. Tutorial review — `docs/playtest/rubrics/tutorial-review.md`

| # | Dimension | Score | Note |
|---|---|---|---|
| 1 | First-touch clarity | 2 | E1/E2 disclosure works, but no diegetic prompt tells the player Directive Panel is next — E3 Silas lines are one-shot |
| 2 | Progressive disclosure | 4 | Panels emerge on use exactly per plan (maturity 1 → 2 → 3) |
| 3 | No-hand-holding pressure | 3 | Silas emerges without overlay tutorial — good; but a first-time player has no "what do I do here" seed |
| 4 | Cold-boot survival | 2 | With one Q1 directive shipped, no fresh player can complete Q1 today — content, not tutorial, is the blocker |
| 5 | Onboarding vs Q_N | 4 | Onboarding stays inside Q1 scope; no drift |

**Highest-leverage change:** Track C1 (Q1 arc scaffold) or a one-line "This is Silas. This is your first directive." seed in the awakening sequence. Cost: S (seed) or L (arc).

#### 14. Polish review — `docs/playtest/rubrics/polish-review.md`

| # | Dimension | Score | Note |
|---|---|---|---|
| 1 | Micro-animation polish | 4 | AlertCrawler fade-in, panel emergence, donut arc reveal all read intentional |
| 2 | Audio/text pacing | 3 | Silence pillar honored (no audio), but no rhythm testing has been done — NR without human |
| 3 | Visual hierarchy | 4 | HUD Mockup parity confirmed via `07-result-01.png` — eye lands on Result card, then Directive, then LogDock |
| 4 | Comfort compliance | 3 | Reduced-motion collapses `.log-entry-enter` to 0 ms; contrast/textSize/narrationPace are Track D, not yet wired at runtime |
| 5 | Empty-state grace | 4 | AlertCrawler `null` on empty ledger; FinancialOverview maturity-gates donut cleanly |
| 6 | Error surfaces | NR | No failure paths exercised in this dry-run |

**Highest-leverage change:** Ship Track D1–D4 comfort runtime wiring. Comfort pillar can't be a §9 pillar if only 1/4 settings do real work. Cost: M.

#### 15. Mechanics review — `docs/playtest/rubrics/mechanics-review.md`

Mechanics in focus since previous baseline: modules registry + rank-aware dispatch (B4), installed-module map (B3), moduleAbility stubs (B2).

| # | Dimension | Score | Note |
|---|---|---|---|
| 1 | Isolation satisfaction | 3 | Dispatch is understandable in isolation; stubs mean the *effect* isn't yet |
| 2 | Depth | NR | B5 (real effects) not yet shipped — depth exists only in intent |
| 3 | MDA coherence | 2 | Mechanics defined, Dynamics not yet realised (stubs), Aesthetics gated on content |
| 4 | Interaction quality | NR | Requires B5 + Track C content |
| 5 | Degenerate strategy risk | NR | Same |

**Highest-leverage change:** Sprint B5 (24 real ModuleAbility effects wired through resolver). Everything downstream — inspections, Silas rapport, module × content interactions — is blocked on this. Cost: L.

#### 16. Feedback-loop review — `docs/playtest/rubrics/feedback-loop-review.md`

Loops touched since previous baseline: HUD-comes-online disclosure loop (E1→E2→E3), maturity-usage ramp.

| # | Dimension | Score | Note |
|---|---|---|---|
| 1 | Amplification vs damping | 4 | Usage → maturity is a mild positive loop; no runaway path |
| 2 | Runaway risk | 5 | Capped at maturity 3; can't overshoot |
| 3 | Signal coherence | 4 | Silas narrates each bump (E3); UI shows the panel expanding |
| 4 | Rubber-band presence | N/A | Not applicable to a monotonic reveal loop |
| 5 | Pacing | 3 | 3-uses-per-stage feels right for a 12-week arc, but 1 shipped directive can't verify |

**Highest-leverage change:** Instrument maturity-bump events to the ledger so post-hoc analysis can see them. Cost: S.

### End-of-content boundary check — **DEFERRED**

Steps 36–45 require walking Q1 to Week 12 on four postures. With only one directive shipped, this walk is not executable. The terminal-flag mechanism (`Q1_CLOSED`) is committed and unit-tested elsewhere; the *behavioral* check needs Track C to land.

Verdict: **BLOCKED.** Analytical scoring is not a substitute for a human Kleenex. Full Phase 4 requires (a) Track C content fill and (b) a fresh human playtester.

---

## Phase 5 — Ship-gate signal check — **HONEST NO**

> Does this build convincingly earn ≥10 unsolicited comments mentioning the **trace ledger** or **character silence**?

**No.**

The trace ledger is present (LogDock + AlertCrawler + result cards form a coherent WHY-NOW spine — a genuinely novel surface). But it currently narrates a single directive loop. Ten unsolicited comments require ten distinct moments a player wants to point at — one directive can't produce that variance.

Character silence is honored structurally (no audio, Silas speaks in text, reduced-motion respected) but is not yet *load-bearing*. The silence-as-horror pillar (§4) requires the player to feel Silas withdraw — which requires directives whose consequences would make Silas withdraw, which requires Track C content.

**Honest read:** V5 was chrome work. It brought the HUD to mockup parity and gave the trace ledger its bottom-edge voice. It did not — and could not — earn the ≥10-signal threshold because that threshold is a *content* threshold, not a *chrome* threshold. This is exactly the case `docs/ship-gate.md:49` describes: "If NO, do not ship — return to `design-discovery` skill and identify what's missing."

What's missing is Track C (Q1 content fill) + Track B5 (real module effects) + Track D (comfort at runtime). Those three tracks, together, are what turns the current chrome-complete slice into a §17-passing slice.

Verdict: **DO NOT SHIP.** Return to plan; execute Track C next.

---

## Phase 6 — Ship — **NOT REACHED**

Per Phase 5 verdict.

---

## Blockers before real RC

1. **Track C** — Q1 12-week directive arc + Q1 inspection scenes. Highest priority (unblocks fun/tutorial/mechanics/feedback-loop scoring, unlocks Phase 4 boundary check, and is the direct §17 signal driver).
2. **Track B5** — 24 real ModuleAbility effects wired through resolver. Unblocks mechanics-review depth score.
3. **Track D1–D4** — comfort at runtime (not just persisted). Unblocks polish-review comfort-compliance score.
4. **webkit engine** — install locally or move RC gate to a CI runner with the full trio.
5. **Soak-500 harness** — either switch to `vite preview` for long soaks or bucket the soak into 5×100 runs.
6. **Human Kleenex playtest** — no AI substitute. 24h code break, one uninterrupted Q1 walk-through.

## Not a blocker (green ticks)

- All mechanical checks (Phase 1) pass with headroom.
- Chromium + firefox cross-browser (Phase 2) is clean.
- 100-iter soak (Phase 1) shows the app is well-behaved on the budget — the 500-iter run's failure is harness-side.
- HUD Mockup parity is confirmed via V5 screenshots (`test-results/v5-chrome-donut-crawler/*.png`).
- Palette lock and colorGuard 4.5:1 contrast pins hold (§9 discipline honored through V5).

---

## Next action

Stop chrome work. Start Track C — Q1 arc outline (Sprint C1). This is what earns Phase 5.

---

## Amendment — 2026-07-08 (post-report)

**What this section is:** an honest correction to Phase 4 / Phase 5 / Next-action language above. Preserved rather than rewritten so the original miss is visible in the record.

### What I got wrong

The original report claimed "only one directive loop (mercyMargin) implemented" and recommended "return to Track C." Both statements are wrong. Filesystem inventory after the report was pushed:

- `game/src/content/tasks/q1/` — **12 authored task files** (week1-mercyMargin through week12-quarter-close-ethics-hearing).
- `game/src/content/choices/q1/` — 12 choice files.
- `game/src/content/consequences/q1/` — 13 consequence files (12 weeks + `week12-quarter-close-terminal.consequences.ts` firing Q1_CLOSED).
- `docs/content/q1-arc.md` — 216-line arc design doc.
- `game/src/content/directiveSchedule.ts` — all 12 weeks wired into `Q1_SEQUENCE`.
- `game/src/tests/content/q1ContentParse.test.ts` — Zod-parses every week; runs on every commit.
- `game/src/content/inspections/` — `q1Payroll.scene.ts`, `q1Ethics.scene.ts`, `q1Inspection.scene.ts` (Track C14 shipped).

Sprints C1 through C14 were completed in prior sessions between when the plan file was authored and when I inherited execution. My mistake was applying the plan file's stated "current state" without re-verifying the code — the CLAUDE.md rule "Before recommending from memory, verify" applies to plan files too, since a plan is a memory of past intent.

### What the real Phase 5 blocker looks like

Content is authored; the runtime scheduler is not consuming it. The V5 stageWalker exited after ~2 iterations because the walker's identity-guard tripped:

```ts
// stageWalker.spec.ts:116
if (directiveText && directiveText === prevDirectiveText) {
  return { advanced: false, directiveText }
}
```

Same directive text on two successive iterations = walker bails. That fires whether the queue truly has no next directive OR the presenter is stuck on week 1. The diagnostic walker run at commit `76facd0` will show which — but the mere existence of authored week 2-12 files does not prove the queue slice is loading them.

### Revised blocker list

1. ~~Track C — Q1 12-week directive arc + Q1 inspection scenes~~ — **Track C is shipped.** Instead: verify the queue slice / scheduler wires `Q1_SEQUENCE` into the live directive presenter, and diagnose why stageWalker's identity guard fires on iteration 2.
2. **Track B5** — 24 real ModuleAbility effects wired through resolver (unchanged from original).
3. **Track D1–D4** — comfort at runtime (unchanged from original).
4. **webkit engine** — install or CI runner (unchanged from original).
5. **Soak-500 harness** — `vite preview` or bucket (unchanged from original).
6. **Human Kleenex playtest** — 24 h break, one uninterrupted Q1 walk-through, using the authored content that already exists.

### Revised Phase 5 verdict

Still **HONEST NO** — but for a different reason. The pillars (trace ledger, character silence) can now be *earned* by the authored Q1 arc that exists. What's blocking the ≥10-signal threshold is (a) verifying the runtime actually delivers the arc end-to-end, then (b) letting a human sit with all 12 weeks in a Kleenex session. The chrome→content misdiagnosis in the original report was the miss; the honest NO stands because a solo AI executor cannot run the Kleenex.

### Diagnosis run 1 — stageWalker with verbose output (`test-results/diagnostic-2026-07-08/`)

Ran `ECHO9_SPRINT=diagnostic-2026-07-08 npx playwright test stageWalker --project chromium`. Result: 1 passed in 14.8 s. Walker captured 2 directives and terminated at "10-terminal-state." State dump at exit showed:

- `flags: {}` (empty Set)
- No `activeDirective` or `currentTask` at top level
- Queue-shaped keys present: `scheduledConsequences`, `currentPromptId`, `currentInspectionSceneIndex`, `currentInspectionKey`, `pendingFiredHooks`

Initial read: "runtime scheduler doesn't set week flags." Wrong. Grep for `Q1_SEQUENCE` in `src/` shows `Layout.tsx:181` and `state/selectors/currentWeek.ts:27` DO consume the sequence — the runtime does pick the next unresolved week off `Q1_SEQUENCE`.

Then the actual diagnosis surfaced from the code itself. `game/src/tests/e2e/fullPlaytest.spec.ts:1-25` documents the walker's known limitation:

> stageWalker.spec.ts walks a *sparse* directive loop that … uses an H2-identity guard that collides with ResultCard's own H2 "Result" — so post-W1 the guard reports "no new directive" and terminates after ~1-2 real weeks resolve … does not press ResultCard's "Continue" button between weeks. Consequence: the C15 12-week arc is not actually walked; only the W1 mercy-margin choice resolves.

**stageWalker is a shallow visual-capture tool**, not a comprehensive playtest. The empty flags at exit reflect that only W1 resolved before the walker bailed on its own heading-identity guard. `fullPlaytest.spec.ts` (the comprehensive replacement) walks Q1_SEQUENCE by flag inspection instead of heading text and is part of the trinity's 645/645.

### Diagnosis run 2 — fullPlaytest headed (`test-results/amendment-2026-07-08/`)

Ran `ECHO9_PLAYTEST=amendment-2026-07-08 npx playwright test fullPlaytest --project chromium --headed`. Result: **1 passed in 48.7 s**, 38 screenshots + state/beat dumps captured. Final state at the last three snapshots (`36-W12-inspection-closed`, `37-week12-terminal-no-continue`, `38-end-of-content-overlay`):

```
flags: [Q1_WEEK1_RESOLVED, Q1_WEEK2_RESOLVED, Q1_WEEK3_RESOLVED, Q1_WEEK4_RESOLVED,
        Q1_WEEK5_RESOLVED, Q1_WEEK6_RESOLVED, Q1_WEEK7_RESOLVED, Q1_WEEK8_RESOLVED,
        Q1_WEEK9_RESOLVED, Q1_WEEK10_RESOLVED, Q1_WEEK11_RESOLVED, Q1_WEEK12_RESOLVED,
        Q1_CLOSED]
phase: INSPECTION
meters: { CAPITAL: -2, HUMAN_WELFARE: -1, OWNER_CONTROL: 15 }
```

- All 12 Q1 week resolution flags fire in sequence.
- `Q1_CLOSED` fires from `week12-quarter-close-terminal.consequences.ts` — the End-of-Content trigger.
- All three inspections open and close (`15-W4-inspection-open`, `25-W8-inspection-open`, `35-W12-inspection-open` + `-closed` pairs).
- End-of-Content overlay renders (`38-end-of-content-overlay.png`).
- Meters land non-trivially (CAP -2, HW -1, OC +15) — the arc actually deltas state; it's not a scripted rail.

**The runtime IS fully wired.** The C-track sprints (C1–C14) shipped both authoring AND runtime plumbing. Cold-boot → 12 weeks → 3 inspections → EoC overlay is a **complete, behavioral, deterministic playtest today**.

### Revised revised Phase 5 verdict

Still **HONEST NO** — but for a much narrower reason than the original report or the first amendment claimed.

The build has:

- A complete Q1 arc that walks end-to-end deterministically (verified by headed playtest).
- A trace ledger that fires + surfaces across 12 weeks + 3 inspections.
- The character silence pillar structurally honored (no audio, Silas speaks in text).

What still blocks ≥10 unsolicited signal on the trace-ledger + character-silence pillars:

1. **No human has sat with 12 weeks of this yet.** The 48.7 s automated walk resolves flags in order; a human at the keyboard walks the same 12 weeks over 45–90 minutes and forms opinions. Only the human walk generates itch-comment signal.
2. **Module effects are still stubs (Track B5).** With 8 modules shipping only their frames + no delta effects, the "installed modules alter your decisions" pillar can't yet vary the second-run experience — which caps replay-desire signal.
3. **Comfort settings persist but don't do runtime work (Track D).** Reduced-motion collapses `.log-entry-enter` to 0 ms via the global CSS media query, but contrast / text-size / narration-pace are not yet at runtime. Accessibility signal requires all four settings live.

The path to ship is roughly `[B5] + [D1–D4] + [human Kleenex]` — not another Track-C sprint.

### Revised revised next action

1. **Human Kleenex playtest of the current build.** With 24 h code break, walk cold boot → Week 12 → EoC overlay, score against fun/tutorial/polish rubrics. Content-driven fun scoring becomes possible for the first time — Fun rows 1–3 (incentive chain, tension, surprise reveals) that were `NR` in this dry-run become ratable.
2. **Sprint B5** — 24 real ModuleAbility effects. Unblocks Mechanics review depth score + replay-desire signal.
3. **Sprint D1–D4** — comfort runtime wiring. Unblocks Polish comfort-compliance and closes the a11y gap.
4. **Only then** revisit the Phase 5 signal check.

The corrected recommendation shortens the path to ship by roughly all of Track C — which was already done — plus roughly all of the "return to design-discovery" retreat implied by the original report. What remains is B5 + D + human playtest.
